# zadanie
1. Skopiuj plik .env.template do .env i dostosuj ustawienia do swojego środowiska: cp .env.template .env
2. Proponowane uruchomienie aplikacji za pomocą docker-compose: docker-compose up --build -d.
3. Serwer nasłuchuje na porcie 8080.